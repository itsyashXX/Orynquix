MAGICCIPHER V6 — TERMUX UPDATE

1. Put magiccipher-v6-fullstack.zip in Android Download.
2. Run:

cd ~/storage/shared/Download
rm -rf ~/magiccipher-v6
mkdir -p ~/magiccipher-v6
unzip -o magiccipher-v6-fullstack.zip -d ~/magiccipher-v6
cd ~/magiccipher-v6/guptlekh-studio
npm install
npm run build

# Only after the build succeeds:
rm -rf .git
git init
git branch -M main
git config user.name "itsyashXX"
git config user.email "itsyashxx@gmail.com"
git add .
git commit -m "Upgrade MagicCipher to V6 adaptive engine"
git remote add origin https://github.com/itsyashXX/magiccipher.git
git push -u origin main --force

This intentionally makes V6 the only project at the repository root so Vercel does not compile old V4/V5 folders.
